#!/bin/bash
string=$1
match1="New message from"
match2="New message in"
if [[ "$string" =~ "$match1" || "$string" =~ "$match2" ]]
  then
    sound=`ls ~/.local/lib/sounds | shuf -n 1`
    echo $sound
    mpg123 -f 2000 ~/.local/lib/sounds/$sound
fi

